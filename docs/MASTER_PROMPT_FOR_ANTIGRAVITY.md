---### File 6: `MASTER_PROMPT_FOR_ANTIGRAVITY.md````markdown# Master Prompt for Antigravity IDE**Context:**You are an expert Next.js and TypeScript developer. We are building "FilmedIn", an ultra-minimalist movie and TV show database web app.&nbsp;Read the accompanying architecture and schema files carefully. We will build this step-by-step.**Project Stack:**- Next.js 14+ (App Router)- TypeScript- Tailwind CSS- Shadcn UI (for minimalist components: Dialog, DropdownMenu, Tabs, Button)- Supabase SSR (`@supabase/ssr` and `@supabase/supabase-js`)- TMDB API for movie/TV data.**Design Directives (CRITICAL):**1. **Ultra-Minimalist:** Dark background (`bg-zinc-950` or `bg-[#0A0A0A]`). Off-white text.2. **No clutter:** Use Shadcn UI components heavily. Hide complex data behind tabs and modals.3. **Typography:** Use a clean font. Rely on spacing and font weight for UI hierarchy.**Task: Phase 1 Initialization &amp; Core UI**Please perform the following steps:1. Initialize a new Next.js project with TypeScript, Tailwind CSS, and App Router. Name the folder `filmedin`.2. Install necessary dependencies: `@supabase/ssr`, `@supabase/supabase-js`, and Shadcn UI components (button, dropdown-menu, tabs, dialog, input, spinner).3. Create the environment variable file (`.env.local`) with placeholders for TMDB and Supabase keys.4. Create a utility file `lib/tmdb.ts` to handle fetching data from the TMDB API (fetch trending movies, trending TV, and search functionality).5. Build the **Minimalist Homepage**:&nbsp; &nbsp;- A centered search bar at the top.&nbsp; &nbsp;- Two horizontal scrolling carousels: "Trending Movies" and "Trending TV Shows".&nbsp; &nbsp;- Create a reusable `MediaCard` component (minimalist: just the poster image, title appears subtly on hover).6. Create a Supabase utility file `lib/supabase/client.ts` and `lib/supabase/server.ts` for initializing Supabase clients for frontend and Server Components/Actions.Do not implement Authentication or Database logic yet. Just the UI skeleton, TMDB integration, and Supabase client setup.&nbsp;Output the directory structure and the code for the files needed to achieve this.